# Build output
bin/
obj/
out/

# Deployment output (contains the production connection string — never commit)
deploy/

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo

# SQLite database + WAL/SHM files (local runtime data, not source)
*.db
*.db-shm
*.db-wal

# Uploaded Step media (FR-4, runtime data, not source)
src/FlowLine.Web/wwwroot/media/*
!src/FlowLine.Web/wwwroot/media/.gitkeep

# User-specific files
*.rsuser

# OS files
.DS_Store
Thumbs.db

# Claude Code local tooling/config (machine-specific, not shared)
.claude/
